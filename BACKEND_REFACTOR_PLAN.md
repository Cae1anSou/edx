# Open edX 后端重构完整方案（Java 主干）

## 1. 文档目标

本文给出当前 `/machine/Learning/Code/edx` 项目的后端重构完整方案，目标是在保留现有前端与业务连续性的前提下，将 Django 单体逐步迁移为 Java（Spring Boot）主导的后端架构。

## 2. 结论与原则

### 2.1 结论
- 重写主语言：**Java（Spring Boot）**。
- 重构路径：**渐进式替换（Strangler Fig）**，而不是一次性重写切换。

### 2.2 原则
- 业务优先：任何阶段不影响核心学习流程可用性（注册、选课、学习、评分、证书）。
- 兼容优先：新旧系统长期并行，逐域切流。
- 可回滚优先：每个迁移里程碑均具备快速回滚机制。
- 数据一致性优先：先保证正确性，再优化性能。

## 3. 当前系统现状（证据）

仓库静态扫描显示这是高度耦合的 Django/Open edX 单体：

- `djangoapps` 目录规模：约 `120`
- 迁移文件：约 `576`
- Celery 任务装饰器引用：约 `260`
- `signals.py` 文件：`31`
- Django admin 注册：约 `185`
- modulestore/Mongo 相关引用：约 `5507`
- ORM 相关引用：约 `910`
- DRF/APIView/ViewSet/Serializer 相关匹配：约 `4007`

说明：该项目不仅是 CRUD 系统，还包含插件扩展、异步任务、事件总线、异构存储（SQL + Mongo/modulestore）与复杂运营后台能力。

## 4. 重构范围

### 4.1 In Scope
- 业务服务后端从 Django 向 Java 迁移。
- API 契约统一（OpenAPI + 错误码 + 鉴权标准）。
- 任务系统从 Celery 逐步迁移到 Java 任务/消息体系。
- 事件总线接入标准化（Kafka/Redis 等按现网能力选择）。
- 可观测性体系统一（日志、指标、追踪、审计）。

### 4.2 Out of Scope（阶段内不做）
- 前端大规模重写。
- 一次性清退全部 Django 模块。
- 先期对所有历史数据做全量重整。

## 5. 目标架构

### 5.1 架构形态
采用“**模块化单体 + 可拆分微服务**”的 Java 架构：

- `platform-gateway`：统一入口、鉴权、灰度路由。
- `identity-service`：用户、认证、权限、组织关系。
- `course-service`：课程元数据、发布状态、目录。
- `enrollment-service`：选课、退课、学习班级关联。
- `learning-progress-service`：学习进度、完成度、里程碑。
- `grading-service`：评分、成绩单、重算流程。
- `certificate-service`：证书生成与撤销。
- `notification-service`：邮件/站内消息/异步通知。
- `job-orchestrator`：异步任务调度、重试、补偿。

> 首期建议作为一个仓库中的多模块 Java 应用部署，减少分布式复杂度；稳定后再拆服务。

### 5.2 数据层策略
- 关系型数据：优先迁移到统一主库（保留原库并做双写校验阶段）。
- modulestore/Mongo：设为“后迁移域”，先通过兼容层适配，不在一期硬迁。
- 读写策略：先读旧写旧 + 新系统影子写；再双写；最后切主。

### 5.3 事件与任务
- 事件模型：定义领域事件规范（名称、版本、幂等键、重放策略）。
- 任务模型：Celery 任务按域迁移到 Java worker（支持重试/死信/告警）。
- 幂等保障：所有跨系统写操作强制幂等 token。

## 6. 迁移路线图（12 个月）

## Phase 0（第 1-4 周）：准备期
- 产出：
  - 领域边界图（按业务域拆分）
  - 系统契约基线（OpenAPI、错误码、鉴权方案）
  - 迁移治理机制（RFC/ADR、变更审批、回滚预案）
- 工程基建：
  - Java 基础工程骨架（Spring Boot、MyBatis/JPA、Flyway、Testcontainers）
  - 统一日志规范、trace id、审计字段
  - CI/CD pipeline + contract test

## Phase 1（第 2-3 月）：低风险域先行
- 优先域：通知、用户偏好、轻量查询接口。
- 迁移方式：
  - 通过网关按 API 维度灰度切流（1% -> 10% -> 50% -> 100%）
  - Django 保持兜底回退路径
- 退出标准：
  - 新服务 2 周无 P1/P2 事故
  - 关键接口错误率不高于旧系统

## Phase 2（第 4-6 月）：核心交易域迁移
- 优先域：选课、课程访问权限、学习进度。
- 关键动作：
  - 引入双写（旧库 + 新库）并做一致性核对
  - 关键业务加补偿任务（防止跨系统局部失败）
  - 建立每日对账任务（进度、选课状态）
- 退出标准：
  - 一致性差异 < 0.1%
  - 读请求 80% 以上由新系统承接

## Phase 3（第 7-9 月）：评分与证书域迁移
- 优先域：成绩计算、成绩重算、证书发放/撤销。
- 风险控制：
  - 平行跑批（新旧并行计算对比）
  - 证书发放先影子模式，后真实发放
- 退出标准：
  - 核心报表与成绩一致性满足业务验收
  - 客诉与工单无显著上升

## Phase 4（第 10-12 月）：遗留域收敛与下线
- 目标：
  - 清理已迁移域 Django 代码与路由
  - 收敛 Celery 任务
  - 只保留暂不迁移的 legacy 兼容层
- 退出标准：
  - 主链路全部由 Java 承载
  - Django 仅剩低频遗留模块（有明确下线计划）

## 7. 关键设计决策

### 7.1 API 与契约治理
- 所有新接口必须先有 OpenAPI。
- 接口兼容策略：新增字段向后兼容，删除字段需经过 deprecation 周期。
- 统一错误码分层：平台级、领域级、第三方依赖级。

### 7.2 鉴权与权限
- 沿用现有 SSO/OAuth 体系，Java 服务通过统一鉴权网关接入。
- 权限模型按“平台角色 + 课程角色 + 组织角色”三层抽象。

### 7.3 数据一致性策略
- 同步链路：事务内 outbox + 异步投递。
- 双写阶段：
  - 主写旧库（初期）
  - 并行写新库并校验
  - 逐步切换主写新库
- 所有关键表建立数据核对任务与异常告警。

### 7.4 可观测性与 SRE
- 指标：QPS、P95/P99、错误率、任务积压、事件延迟。
- 日志：结构化日志（request_id、user_id、course_id、trace_id）。
- 追踪：全链路 tracing 覆盖网关 -> 服务 -> DB/MQ。
- SLO：核心学习链路可用性 >= 99.9%。

## 8. 组织与人力建议

### 8.1 团队配置（建议）
- 架构负责人 x1
- Java 后端工程师 x4~6
- Django 维护/迁移工程师 x2~3
- QA/自动化测试 x2
- SRE/DevOps x1~2
- 数据工程/DBA x1

### 8.2 协作机制
- 每周一次迁移评审会（架构、业务、运维）。
- 双周发布节奏，固定灰度窗口。
- 每月一次迁移 KPI 复盘（性能、稳定性、一致性）。

## 9. 风险清单与应对

- 风险：领域边界切分错误导致跨域耦合持续放大。
  - 应对：Phase 0 完成事件风暴与领域建模评审，ADR 固化。
- 风险：双写期间数据漂移。
  - 应对：每日对账 + 自动补偿 + 人工审计通道。
- 风险：Celery 迁移后任务语义偏差。
  - 应对：关键任务并行运行一段时间，逐任务验收后切换。
- 风险：迁移周期过长导致团队疲劳。
  - 应对：按域交付可见价值，每阶段必须有业务收益。
- 风险：大促/开课高峰窗口发生回归。
  - 应对：冻结期制度，峰值前禁止核心链路大变更。

## 10. 验收标准（Definition of Done）

- 技术指标：
  - 核心接口 P95 与错误率不劣于旧系统。
  - 数据一致性达到约定阈值并稳定达标。
- 业务指标：
  - 核心用户流程（注册、选课、学习、评分、证书）无额外阻塞。
  - 运营与客服工单量不因迁移显著升高。
- 运维指标：
  - 回滚演练通过。
  - 关键故障有 runbook 与 oncall 机制。

## 11. 前 30 天落地清单（立即执行）

1. 建立 Java 主工程与统一代码规范（含 CI、测试基线）。
2. 输出迁移 RFC-001：边界拆分与总体路线（评审通过）。
3. 建立 API 网关灰度路由能力（按 URI / Header / 用户分群）。
4. 选定第一个低风险域（建议通知或用户偏好）并完成端到端迁移。
5. 建立双写对账框架（哪怕首域先空跑），提前验证机制。
6. 建立迁移看板：每域状态、风险、负责人、目标日期。

## 12. 附录：推荐技术栈（Java）

- Framework: Spring Boot 4.x
- API: Spring Web + Spring Validation + springdoc-openapi
- Persistence: MyBatis 或 Spring Data JPA（按域选择）
- Migration: Flyway
- Messaging: Kafka（主）/ Redis Stream（过渡）
- Task: Spring Scheduler + MQ Worker
- Cache: Redis
- Observability: OpenTelemetry + Prometheus + Grafana + ELK
- Testing: JUnit5 + Testcontainers + Pact（契约测试）

---

## 版本记录
- `v1.0`（2026-02-27）：首版完整重构方案，基于当前仓库结构评估。
